const byteSize = (str) => {
  // write your code here
	// let count = 0;
	let val =  str.value;
	let leng = val.length;
		return leng;
	
	
};

// Do not change the code below
const str = prompt("Enter some string.");
alert(byteSize(str));
